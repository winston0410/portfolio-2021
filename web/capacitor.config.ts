import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hugosum.me',
  appName: 'portfolio-2021',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
